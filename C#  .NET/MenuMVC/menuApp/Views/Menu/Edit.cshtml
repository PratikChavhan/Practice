@using BOL;
@using BLL;
@{
    ViewData["Title"] = "Update";
}
<form method="post">
    <div>
        <input name="id" type="number" value="@Model.id" readonly>
        <input name="name" placeholder="@Model.name" type="text" value="@Model.name">
        <input name="rate" placeholder="@Model.rate" type="number" value="@Model.rate">

        <button type="submit">Update</button>
    </div>
</form>